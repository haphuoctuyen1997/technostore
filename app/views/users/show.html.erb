<% provide(:title, @user.name) %>        
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= t ".full_name" %></th>
        <th><%= t ".email" %></th>
        <th><%= t ".address" %></th>
        <th><%= t ".phone" %></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @user.name %></td>
        <td><%= @user.email %></td>
        <td><%= @user.address %></td>
        <td><%= @user.phone %></td>
        <td><%= link_to t(".edit"), edit_user_path %></a></td>
      </tr>
    </tbody>
  </table>
